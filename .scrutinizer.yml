build:
    tests:
        override:
            -
                command: 'vendor/bin/phpunit --coverage-clover=coverage.txt'
                coverage:
                    file: 'coverage.txt'
                    format: 'clover'
